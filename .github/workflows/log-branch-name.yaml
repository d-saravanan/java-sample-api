name: Log Branch Name
on: [push, pull_request]

jobs:
  log-branch-name:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Log Branch Name
        run: echo "Current Branch: ${{ github.head_ref || github.ref##refs/heads/ }}"
